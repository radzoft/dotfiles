# ~/.bashrc.secrets — NOT tracked in git (copy from template and fill in)
# This file is sourced at the end of ~/.bashrc
#
# Usage:
#   cp ~/dotfiles/home/.bashrc.secrets.template ~/.bashrc.secrets
#   chmod 600 ~/.bashrc.secrets
#   # Edit with your actual keys

# ── API Keys ──────────────────────────────────────────────────────────────────
export ELEVENLABS_API_KEY=""
export OPENAI_API_KEY=""
export ANTHROPIC_API_KEY=""
export GITHUB_TOKEN=""

# ── Other secrets ─────────────────────────────────────────────────────────────
# export SOME_SERVICE_API_KEY=""
