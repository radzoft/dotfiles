# DNF packages — curated list (repos added by install-packages.sh)
# Last snapshot: Fedora 42

# ── Shell & CLI ───────────────────────────────────────────────────────────────
# Note: gh, git-delta, fzf, ripgrep, fd, jq, yq, zoxide, lazygit → managed by mise
# Note: bun, node, go, uv → managed by mise
bash
bash-completion
stow
git
curl
wget2-wget
bc
htop
btop
inotify-tools
lsof
tree
unzip
zip

# ── Terminal ──────────────────────────────────────────────────────────────────
# ghostty → installed via flatpak (com.mitchellh.ghostty) — not in Fedora repos
alacritty

# ── Editors & IDEs ────────────────────────────────────────────────────────────
code
code-insiders

# ── Container & Virtualisation ────────────────────────────────────────────────
docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin
podman
distrobox
virt-manager
libvirt

# ── GNOME Desktop ─────────────────────────────────────────────────────────────
gnome-tweaks
dconf-editor
gnome-pomodoro
easyeffects
flameshot
gnome-browser-connector
gnome-shell-extension-background-logo

# ── Network & Security ────────────────────────────────────────────────────────
# Note: cloudflared runs as a system service — keep in DNF
tailscale
cloudflared
wireguard-tools
nmap-ncat
openssh-clients

# ── Python ────────────────────────────────────────────────────────────────────
# python managed by mise (python = "3.13"); uv handles script venvs.
# python3-pyserial no longer needed — uv installs it in an isolated venv for ttgo2.py.

# ── Dev libraries ─────────────────────────────────────────────────────────────
gcc
make

# ── DKMS (required for it87 out-of-tree kernel module) ───────────────────────
dkms
kernel-devel

# ── Filesystem ────────────────────────────────────────────────────────────────
btrfs-progs
btrfs-assistant
snapper

# ── Productivity ─────────────────────────────────────────────────────────────
libreoffice-writer

# ── Media ─────────────────────────────────────────────────────────────────────
pipewire-utils
pipewire-alsa
pipewire-pulseaudio

# ── Drivers & Firmware ────────────────────────────────────────────────────────
amd-gpu-firmware
amd-ucode-firmware

# ── Proprietary / Third-party (repos added separately) ───────────────────────
# slack                  # add slack repo: https://slack.com/intl/en-gb/downloads/linux
# zoom                   # add zoom repo: https://zoom.us/download
# wps-office             # add wps repo
# rustdesk               # https://github.com/rustdesk/rustdesk/releases
# pgadmin4-desktop       # https://www.pgadmin.org/download/pgadmin-4-rpm/
